## Design Context

This project has `PRODUCT.md` (strategy/register/users) and `DESIGN.md` (visual system: colors, typography, components) at the repo root, written via `/impeccable init` + `/impeccable document`. Read both before making UI/UX changes to apps/main or apps/admin. Register: **product**. Shared brand accent (deep navy "Vaandelmarine", 258° hue) is deliberately identical to `apps/teamindeling`'s own accent — see DESIGN.md's "Shared-Family Rule".
