# apps/trainingen heeft geen DATABASE_URL — oefeningen staan als markdown in
# content/oefeningen en trainingen staan lokaal (localStorage). De main API
# wordt alleen gebruikt voor de app-lijst in de toolbar.
MAIN_APP_URL="http://localhost:3000"
