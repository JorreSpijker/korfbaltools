# apps/vastspelen has no DATABASE_URL — it only talks to the main API (see docs/plan.md section 10),
# which is where both the vastspelen data and the toolbar's app list come from.
MAIN_APP_URL="http://localhost:3000"

# Google Analytics measurement ID (e.g. G-XXXXXXXXXX). Leave empty to disable GA.
NEXT_PUBLIC_GA_ID=""
